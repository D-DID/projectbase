# 데모 화면 + 수동입력 검증 end-to-end (적용 안내)

## 적용

1. 이 ZIP 을 프로젝트 루트에 압축 해제 (덮어쓰기)
2. MySQL 에서 `src/main/resources/sample-data.sql` 실행
   (IntelliJ Database 패널 → `recallcheck` 우클릭 → SQL Scripts → Run SQL Script...)
3. 앱 재시작
4. 브라우저에서 **http://localhost:8080** 열기

## 데모 시나리오 (그대로 따라하면 됨)

1. 화면 상단에서 **회원가입** → **로그인** (기본값 그대로 두면 됨)
2. 아래 값 중 하나를 넣고 **검증하기**

| 입력 | 기대 판정 |
|---|---|
| 제품명 `가정용 물티슈` · 모델명 `WT-100C` | **일치** (빨강) |
| 모델명 `WT100C` (하이픈 없이) | **일치** — 정규화가 동작하는 것을 보여줌 |
| 제품명 `물티슈` 만 | **부분 일치** (주황) |
| 인증번호 `HU07-123456` 만 | **일치** |
| 제품명 `무선 이어폰` | **불일치** (초록) |
| 전부 비워두고 검증 | **확인 불가** (회색) — 억지 판정하지 않음 |

3. 결과 카드에 4단계 판정 배지, 종합 유사도, 리콜 상세, 판정 근거가 표시됨
4. **내 검증 이력** 버튼으로 FR-015 확인

## 데모에서 반드시 말할 것

- `recall` 테이블 데이터는 **개발 검증용 샘플**이며 실제 공표 데이터가 아님
  (화면 하단에도 노란 박스로 명시해 뒀음)
- Open API 인증키는 발급 완료, 실데이터 적재(FR-009·016)는 다음 작업
- URL 파싱(FR-005) · 이미지 OCR(FR-006)은 미구현 — 호출하면 501 로 명확히 응답함

## 이번에 구현된 것

| FR | 내용 | 상태 |
|---|---|---|
| FR-001, 002 | 회원가입 · 로그인 (JWT) | 동작 |
| FR-007 | 식별 정보 통합 (IdentityMerger) | 동작 |
| FR-008 | 사용자 직접 입력 | 동작 |
| FR-009 | 캐시에서 리콜 후보 조회 | 동작 |
| FR-010 | 항목별 유사도 매칭 (가중치 적용) | 동작 |
| FR-011 | 4단계 판정 | 동작 |
| FR-012 | 항목별 대조 | 동작 |
| FR-013 | 검증 결과 제공 | 동작 |
| FR-014 | 판정 근거 제공 | 동작 |
| FR-015 | 검증 이력 조회 | 동작 |
| FR-005, 006 | URL 파싱 / OCR | 미구현 (501 응답) |
| FR-016, 017 | 동기화 | 미구현 |

## 매칭 로직 요약 (질문 대비)

- **정규화**: 공백·하이픈·괄호·특수문자 제거 후 대문자 통일 → `WT-100C` 와 `WT100C` 가 같아짐
- **콤마 목록 대응**: `recall.recall_model_name` 은 콤마 구분 목록이라 split 후 개별 비교, 최고 점수 채택
- **유사도**: 정규화 편집거리(Levenshtein). 포함 관계면 0.75~0.95 로 가산
- **가중치**: 모델명 0.50 / 인증번호 0.25 / 제품명 0.15 / 제조사 0.10 — `application.properties` 에서 조정
- **임계값**: 0.85 이상 일치, 0.60 이상 부분 일치 — **아직 라벨링 데이터로 튜닝하지 않은 잠정값**
- 양쪽에 값이 있는 항목만 분모에 포함 — 입력하지 않은 항목 때문에 점수가 깎이지 않음
