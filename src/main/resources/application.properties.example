 이 파일은 .gitignore.*spring.application.name=recallcheck
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ============================================================================
 이 파일은 .gitignore.*# 이 파일은 .gitignore 에 등록되어 있어 git 에 올라가지 않는다.
 이 파일은 .gitignore.*# 실제 인증키·비밀번호는 여기에만 두고, 팀 공유용 템플릿은
 이 파일은 .gitignore.*# application.properties.example 을 수정할 것.
 이 파일은 .gitignore.*# ============================================================================
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- DataSource
 이 파일은 .gitignore.*spring.datasource.url=jdbc:mysql://localhost:3306/recallcheck?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
 이 파일은 .gitignore.*spring.datasource.username=root
 이 파일은 .gitignore.*spring.datasource.password=여기에_DB_비밀번호
 이 파일은 .gitignore.*spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- JPA
 이 파일은 .gitignore.*# schema.sql 로 스키마를 만든 뒤에는 validate 로 두어 엔티티와 실제 스키마가 갈라지지 않게 한다.
 이 파일은 .gitignore.*spring.jpa.hibernate.ddl-auto=validate
 이 파일은 .gitignore.*spring.jpa.open-in-view=false
 이 파일은 .gitignore.*spring.jpa.properties.hibernate.show_sql=true
 이 파일은 .gitignore.*spring.jpa.properties.hibernate.format_sql=true
 이 파일은 .gitignore.*spring.jpa.properties.hibernate.use_sql_comments=true
 이 파일은 .gitignore.*# 리콜 데이터 대량 적재용 배치 설정
 이 파일은 .gitignore.*spring.jpa.properties.hibernate.jdbc.batch_size=100
 이 파일은 .gitignore.*spring.jpa.properties.hibernate.order_inserts=true
 이 파일은 .gitignore.*spring.jpa.properties.hibernate.order_updates=true
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- 파일 업로드 (FR-004)
 이 파일은 .gitignore.*spring.servlet.multipart.max-file-size=10MB
 이 파일은 .gitignore.*spring.servlet.multipart.max-request-size=10MB
 이 파일은 .gitignore.*app.upload.dir=./uploads
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- JWT
 이 파일은 .gitignore.*jwt.secret.key=여기에_Base64_인코딩된_32바이트_이상_시크릿
 이 파일은 .gitignore.*jwt.expiration_time=3600000
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- 국가기술표준원 SafetyKorea Open API (FR-009, FR-016)
 이 파일은 .gitignore.*# 인증키는 쿼리 파라미터가 아니라 HTTP 헤더 AuthKey 로 전송한다 (대소문자 구분).
 이 파일은 .gitignore.*# 목록 조회는 페이징 파라미터가 없고 최대 1,000건까지만 응답한다.
 이 파일은 .gitignore.*openapi.safety-korea.base-url=http://www.safetykorea.kr
 이 파일은 .gitignore.*openapi.safety-korea.auth-key=여기에_발급받은_서비스ID
 이 파일은 .gitignore.*openapi.safety-korea.recall-list-path=/openapi/api/recall/recallList.json
 이 파일은 .gitignore.*openapi.safety-korea.recall-detail-path=/openapi/api/recall/recallDetail.json
 이 파일은 .gitignore.*openapi.safety-korea.cert-list-path=/openapi/api/cert/certificationList.json
 이 파일은 .gitignore.*openapi.safety-korea.cert-detail-path=/openapi/api/cert/certificationDetail.json
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- OCR 모듈 (FR-006)
 이 파일은 .gitignore.*ocr.base-url=http://localhost:8000
 이 파일은 .gitignore.*
 이 파일은 .gitignore.*# ---------------------------------------------------------------- 매칭 가중치·임계값 (FR-010, FR-011)
 이 파일은 .gitignore.*# 확정값 아님. 라벨링 데이터셋으로 튜닝한 뒤 이 값만 바꾼다. 코드에 하드코딩 금지.
 이 파일은 .gitignore.*matching.weight.model-name=0.50
 이 파일은 .gitignore.*matching.weight.cert-num=0.25
 이 파일은 .gitignore.*matching.weight.product-name=0.15
 이 파일은 .gitignore.*matching.weight.maker-name=0.10
 이 파일은 .gitignore.*matching.threshold.match=0.85
 이 파일은 .gitignore.*matching.threshold.partial=0.60
